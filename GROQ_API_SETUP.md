# Get FREE Groq API Key (2 minutes)

## Why Groq?
- ✅ **Completely FREE** - No credit card required
- ✅ **14,400 requests per day** - More than enough
- ✅ **Super FAST** - Fastest AI inference
- ✅ **Mixtral-8x7B** - High quality responses

## Steps to Get API Key

### 1. Go to Groq Console
Visit: https://console.groq.com/

### 2. Sign Up (FREE)
- Click "Sign Up" or "Get Started"
- Use Google/GitHub login (fastest)
- Or create account with email

### 3. Create API Key
- Once logged in, go to "API Keys" section
- Click "Create API Key"
- Give it a name (e.g., "BlueTrace")
- Copy the key (starts with `gsk_...`)

### 4. Add to Project
Open `server/.env` file and add:

```env
GROQ_API_KEY=gsk_your_actual_key_here
```

### 5. Restart Server
```bash
cd server
npm start
```

## Done! 🎉

Now your AI videos will be generated with:
- **Real AI-generated scripts** in any language (Hindi, Marathi, Odia, etc.)
- **Animated video scenes** with beautiful gradients
- **Auto-playing scenes** every 5 seconds
- **Progress bar and controls**

## Fallback Mode

If you don't add the API key, the system will use:
- Pre-written educational content
- Still creates animated videos
- Works perfectly fine!

The API key just makes the content **more personalized** to each topic and language.

## Cost

**$0.00** - Completely free forever!

Groq offers this free tier to promote AI development. No hidden costs, no credit card needed.
